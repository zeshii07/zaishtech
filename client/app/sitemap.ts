import type { MetadataRoute } from 'next'; import { portfolioProjects } from '@/lib/portfolio';
export default function sitemap():MetadataRoute.Sitemap{const base='https://zaishtech.vercel.app';const routes=['','/about','/services','/work','/industries','/process','/pricing','/contact','/blog','/careers','/privacy-policy','/terms'];return [...routes.map(route=>({url:`${base}${route}`,lastModified:new Date(),changeFrequency:route===''?'weekly' as const:'monthly' as const,priority:route===''?1:0.7})),...portfolioProjects.map(p=>({url:`${base}/work/${p.slug}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:0.7}))]}

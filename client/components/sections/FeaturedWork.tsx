import Link from 'next/link';
import { projectImages, realProjects } from '@/lib/portfolio';
export default function FeaturedWork(){return <section id="work" className="py-24 md:py-32 bg-white"><div className="max-w-7xl mx-auto px-6 md:px-12"><div className="flex flex-col md:flex-row md:items-end justify-between gap-6"><div className="max-w-3xl"><span className="text-xs font-bold uppercase tracking-widest text-brand-600">Selected Work</span><h2 className="mt-4 text-3xl md:text-5xl font-medium tracking-tight leading-[1.1]">Products built around<br/><span className="font-serif italic text-stone-400">real business needs</span></h2></div><Link href="/work" className="text-sm font-semibold text-brand-600">Explore all work →</Link></div><div className="mt-16 grid md:grid-cols-2 gap-8">{realProjects.slice(0,4).map(project=><Link href={`/work/${project.slug}`} key={project.slug} className="group rounded-2xl border border-stone-200 bg-stone-50 overflow-hidden hover:-translate-y-1 hover:shadow-xl transition-all"><div className="relative aspect-[16/9] overflow-hidden"><img src={projectImages[project.slug]} alt={`${project.industry} project visual`} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"/><div className="absolute inset-0 bg-gradient-to-t from-stone-950/80 via-transparent to-transparent"/><div className="absolute bottom-0 p-7 text-white"><span className="text-xs uppercase tracking-widest text-brand-300">{project.industry}</span><div className="mt-2 text-2xl font-medium">{project.service}</div></div></div><div className="p-7"><div className="flex items-center justify-between gap-4"><h3 className="text-xl font-medium">{project.title}</h3><span className="text-xs font-semibold text-emerald-700 bg-emerald-50 px-3 py-1 rounded-full">{project.status}</span></div><p className="mt-3 text-sm text-stone-500 leading-relaxed">{project.summary}</p><div className="mt-5 flex flex-wrap gap-2">{project.tech.map(item=><span key={item} className="text-xs bg-white border border-stone-200 rounded-full px-3 py-1 text-stone-600">{item}</span>)}</div></div></Link>)}</div></div></section>}
